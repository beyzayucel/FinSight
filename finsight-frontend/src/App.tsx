import { APP_NAME } from '@/lib/utils'

export default function App() {
  return (
    <div className="grid min-h-screen place-items-center">
      <h1 className="text-4xl font-bold text-emerald-600">{APP_NAME}</h1>
    </div>
  )
}